import { Global } from "@emotion/react"
const Fonts = () => (
    <Global
        styles={`@import url('https://fonts.googleapis.com/css2?family=Spline+Sans+Mono:wght@700&display=swap');`}
    />
)

export default Fonts
